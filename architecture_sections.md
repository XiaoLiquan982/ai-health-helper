# 4.2 系统架构设计

系统采用典型的分层架构模式，将复杂的医疗AI助手系统划分为表现层、业务逻辑层、数据层和技术支撑层四个相对独立又相互协调的层次。各层通过明确定义的接口进行交互，确保系统的高内聚、低耦合，提高了系统的可维护性和可扩展性。

## 4.2.1 表现层

表现层是系统与用户进行交互的最前端，主要负责呈现用户界面并处理用户的交互请求。本系统的表现层采用前后端分离的架构设计，前端采用Vue.js框架结合Element Plus UI组件库开发，构建了一个响应式、交互友好的医疗咨询界面。前端应用通过ChatWindow组件实现了聊天窗口的核心功能，包括消息展示、输入管理和实时流式响应渲染。用户输入的问诊内容通过RESTful API请求方式发送至后端，后端采用Spring Boot框架在XiaoyiController控制器中提供了"/xiaoyi/chat"端点，该端点接收前端POST请求并携带ChatForm对象（包含会话内存标识和用户消息内容），然后通过HTTP流式传输将后端处理结果实时推送给前端进行渲染。表现层的设计充分考虑了医疗场景的特殊性，采用流式传输技术使用户能够实时看到AI医疗助手的响应过程，增强了交互的连贯性和用户体验，特别是对于长篇幅的医疗咨询回复能够分段显示，避免用户的长时间等待。

## 4.2.2 业务逻辑层

业务逻辑层是系统的核心处理层，承载了医疗AI对话的主要业务逻辑，通过与大语言模型的集成实现了智能医疗咨询功能。在该层的设计中，系统定义了两个核心的AI服务接口：Assistant接口和XiaoyiAgent接口。Assistant接口使用@AiService注解，配置了通义千问（Qwen）模型作为聊天模型，提供了基础的单轮对话能力。XiaoyiAgent接口在Assistant基础上进行了增强，通过LangChain4j框架的@AiService注解配置，集成了多项企业级能力：首先配置了流式聊天模型（streamingChatModel）以支持实时流式响应，这使得医疗咨询的回复能够逐字呈现给用户；其次通过ChatMemoryProvider配置了对话记忆管理，系统为每个会话分配唯一的memoryId，利用MessageWindowChatMemory维护最近50条消息记录，并通过MongoDB作为后端存储实现了跨会话的记忆持久化，确保用户在多次对话中能获得连贯的医疗咨询体验；再次通过contentRetriever配置集成了检索增强生成（RAG）功能，系统利用Pinecone向量数据库存储医院相关知识的向量表示，包括医院基本信息、科室介绍、专科信息等知识库内容，在回答用户问题时能够检索最相关的医疗知识进行上下文补充，大大提高了回答的准确性和专业性；最后通过tools配置集成了AppointmentTools工具函数，这些工具函数能够在对话中被自动调用以执行挂号预约等具体医疗业务操作。业务逻辑层还定义了系统提示词模板（xiaoyi-prompt-template.txt），该模板为XiaoyiAgent定义了医疗助手的角色和行为准则，确保AI在医疗场景中的回复始终符合医疗专业要求。

## 4.2.3 数据层

数据层是系统的数据存储和访问层，负责管理系统运行中产生的各类数据的持久化存储和高效检索。本系统采用多数据库协同的架构设计来满足不同类型数据的存储需求。首先，系统采用MySQL数据库存储结构化的业务数据，主要包括挂号预约信息存储在Appointment实体表中，其中包含患者姓名、身份证号、就诊科室、预约日期和时间、医生名称等关键字段，这些数据通过MyBatis-Plus框架进行持久化映射和CRUD操作管理。其次，系统采用MongoDB作为会话对话记录的专用存储引擎，通过自定义的MongoChatMemoryStore实现了聊天历史的灵活存储方案，每个用户会话的所有消息记录都被持久化到MongoDB中，支持跨会话的历史回溯和用户行为分析。再次，系统采用Pinecone云向量数据库存储医疗知识的向量表示，系统将医院信息、科室信息、专科介绍等非结构化文本知识通过embedding模型转换为向量向量，存储在名为"xiaoyi-index"的Pinecone索引的"xiaoyi-namespace"命名空间中，当用户提问时系统能够通过向量相似度检索找到最相关的医疗知识片段，支持RAG功能的高效实现。数据层通过这种多源异构数据库的协调使用，既保证了结构化业务数据的完整性和一致性，又通过向量存储支持了复杂的语义检索能力，为医疗知识的精准匹配和个性化服务提供了坚实基础。

## 4.2.4 技术支撑层

技术支撑层为整个系统提供了必要的技术基础设施和公共能力支撑，包括大语言模型接入、向量模型处理、配置管理等核心技术组件。在大语言模型接入方面，系统通过LangChain4j框架与多种大语言模型进行集成适配，主要使用的是阿里通义千问（Qwen）模型系列和DeepSeek模型，这些模型通过OpenAI标准API接口进行调用，系统在application.properties配置文件中灵活配置了不同模型的endpoint、API Key和模型名称等参数，支持快速切换不同的LLM提供商，同时保留了本地Ollama模型的集成选项用于离线测试。在向量嵌入模型方面，系统采用阿里DashScope提供的text-embedding-v3模型，将医疗知识文本转换为高维向量表示以支持语义检索。在配置管理方面，系统采用Java的注解配置模式和Spring Framework的依赖注入机制，分别在MemoryChatAssistantConfig、XiaoyiAgentConfig和EmbeddingStoreConfig三个配置类中集中管理了对话记忆、AI Agent和向量存储的配置逻辑。在工具函数方面，系统通过LangChain4j的tools机制集成了AppointmentTools和CalculatorTools等业务工具函数，这些工具函数在AI对话过程中可被自动调用以执行具体的医疗业务操作。此外，技术支撑层还包括了基础框架层面的支撑，包括Spring Boot web框架提供的HTTP服务能力、MyBatis-Plus提供的ORM框架能力、Knife4j/Swagger提供的API文档管理能力等，这些技术组件共同构成了一个完整的、可扩展的技术基础设施体系，为业务逻辑层的稳定运行提供了强有力的技术保障。

---

## 架构总结

四层架构通过清晰的分离和高效的协作，构建了一个功能完整、技术先进的医疗AI助手系统。表现层提供友好的交互界面，业务逻辑层实现复杂的医疗AI推理能力，数据层支持多源数据的统一管理，技术支撑层提供可靠的技术基础。这种分层设计使得系统具有良好的可维护性、可扩展性和高可用性，为医疗咨询服务的高质量提供奠定了坚实基础。
